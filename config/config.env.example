NODE_ENV=development
PORT=5000

# Change this to your Netlify URL when deploying to Render (change it in Render's environment variables, not here)
FRONTEND_URL=http://localhost:5173

MONGO_URI=<your mongo URI>

JWT_SECRET=<your JWT secret>
JWT_EXPIRE=30d
JWT_COOKIE_EXPIRE=30
